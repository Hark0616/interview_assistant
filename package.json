{
  "name": "interview-assistant",
  "private": true,
  "description": "Extensión Chrome — Interview Assistant AI (lint solo para desarrollo)",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "verify": "npm run lint && npm test -- --run && node scripts/validate-extension.mjs",
    "vault": "node scripts/vault-server.mjs",
    "test": "vitest"
  },
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "eslint": "^9.21.0",
    "globals": "^15.15.0",
    "jsdom": "^24.1.3",
    "vitest": "^2.1.9"
  }
}
